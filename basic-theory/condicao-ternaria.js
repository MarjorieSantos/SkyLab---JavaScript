//ternário

let returno = (sexo === 'M') ? 'masculino' : 'feminino';

console.log(retorno)


//reduzindo um código

let sexo = 'M';
if (sexo === 'M') {
    return 'masculino';
} else {
    return 'feminino';
}